spring:
  datasource:
    url: jdbc:mysql://localhost:3306/contacts
    username: root
    password: Uzma
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # or create-drop if you want to recreate the database on every start
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
